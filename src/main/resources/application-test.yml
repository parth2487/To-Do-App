spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tododb
    username: todouser
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true